*.png
.DS_Store
*.pyc
__pycache__/
# an optional local symlink to an outside MIB checkout (deps.find_mib_path fallback)
MIB-circuit-track
# scripts/setup.sh clones our MIB fork here at a pinned commit (see src/matryoshka_attribution/deps.py)
deps/MIB-circuit-track/
# goodfire-ai/arithmetic-wild's generated datasets (7 MB), read by scripts/sva/eval_sva.py via
# $MATTR_ARITH_DIR or this path; copied here from Tilde on 2026-09-15, not vendored.
deps/arithmetic-wild/
# per-group uv environments (pyproject.toml [dependency-groups]: lm -> .venv, vit -> .venv-vit, tl2 -> .venv-tl2)
.venv*/
results/
logs/
logs_*/
plots/*
!plots/*.py
.claude/
paper/
*.log

# wandb run dirs (eval_sva.py --wandb); the run of record is on wandb.ai, not on disk
wandb/
# scripts/vw/vw_data.py regenerates these (tok4096.json, train.bin, val.bin, meta.json)
data/vw/
CLAUDE.md
